---
const steps = [
  {
    number: "01",
    title: "We Build Your AI Agent",
    description: "We set up a custom AI voice agent trained on your business, your services, and your voice. It knows what questions to ask and how to book the right jobs."
  },
  {
    number: "02",
    title: "AI Handles Every Conversation",
    description: "Calls, texts, follow-ups - your AI agent works 24/7. It answers, qualifies, and books appointments directly to your calendar."
  },
  {
    number: "03",
    title: "You Show Up and Close",
    description: "Your calendar fills up with qualified appointments. You focus on doing the work and getting paid."
  }
];
---

<section id="how-it-works" class="py-20 lg:py-32 bg-bg-alt relative overflow-hidden">
  <div class="absolute top-0 left-0 w-1/2 h-1/2 bg-gradient-to-br from-accent/10 to-transparent rounded-br-full blur-3xl"></div>
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="text-center mb-20">
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-primary mb-6 text-balance">
        Three Steps to a Full Calendar
      </h2>
    </div>

    <div class="space-y-16 lg:space-y-20">
      {steps.map((step, index) => (
        <div class={`flex flex-col lg:flex-row gap-8 lg:gap-16 items-center ${index % 2 === 1 ? 'lg:flex-row-reverse' : ''}`}>
          <div class="flex-1">
            <div class="relative inline-block">
              <span class="absolute -left-4 -top-4 text-8xl lg:text-9xl font-bold text-accent/10">{step.number}</span>
              <div class="relative">
                <h3 class="text-2xl lg:text-3xl font-bold text-primary mb-4">{step.title}</h3>
                <p class="text-lg text-text-muted leading-relaxed max-w-xl">{step.description}</p>
              </div>
            </div>
          </div>
          
          <div class="flex-1 w-full">
            <div class="aspect-video rounded-2xl bg-white border border-gray-200 shadow-xl overflow-hidden">
              <div class="w-full h-full bg-gradient-to-br from-primary/5 to-accent/5 flex items-center justify-center">
                <div class="text-center p-8">
                  <div class="w-20 h-20 mx-auto mb-4 rounded-full bg-white shadow-lg flex items-center justify-center">
                    <span class="text-3xl font-bold text-action">{step.number}</span>
                  </div>
                  <p class="text-sm text-text-muted font-medium">Visual Demo Placeholder</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <div class="text-center mt-16">
      <a href="#" class="inline-flex items-center gap-2 px-8 py-4 rounded-xl bg-accent text-primary font-bold text-lg hover:bg-accent/90 shadow-lg shadow-accent/20 hover:scale-105 transition-all duration-200">
        See It In Action
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/>
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
      </a>
    </div>
  </div>
</section>
